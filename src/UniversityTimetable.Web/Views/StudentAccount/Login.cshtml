@{
    Layout = null;
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Student Login - University Timetable Portal</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css" />
    <style>
        body { font-family: 'Inter', sans-serif; background: linear-gradient(135deg, #1e3a8a 0%, #3b82f6 100%); min-height: 100vh; display: flex; align-items: center; justify-content: center; }
        .login-card { background: #fff; border-radius: 16px; width: 100%; max-width: 440px; box-shadow: 0 20px 25px -5px rgba(0,0,0,0.3); overflow: hidden; }
        .login-header { background: #2563eb; color: #fff; padding: 2.5rem 2rem; text-align: center; }
    </style>
</head>
<body>
    <div class="login-card">
        <div class="login-header">
            <i class="fa-solid fa-graduation-cap fa-3x mb-2"></i>
            <h4 class="fw-bold mb-1">STUDENT PORTAL</h4>
            <p class="mb-0 opacity-75 small">Personalized Academic Schedule & Class Directory</p>
        </div>
        <div class="p-4 p-md-5">
            @if (ViewBag.Error != null)
            {
                <div class="alert alert-danger small py-2 mb-4"><i class="fa-solid fa-circle-exclamation me-1"></i>@ViewBag.Error</div>
            }
            <form action="/Student/Account/Login" method="post">
                <div class="mb-3">
                    <label class="form-label fw-semibold">Institutional Student Email</label>
                    <div class="input-group">
                        <span class="input-group-text bg-light"><i class="fa-solid fa-envelope text-muted"></i></span>
                        <input type="email" name="email" class="form-control" value="student@university.edu" required />
                    </div>
                </div>
                <div class="mb-4">
                    <label class="form-label fw-semibold">Password</label>
                    <div class="input-group">
                        <span class="input-group-text bg-light"><i class="fa-solid fa-lock text-muted"></i></span>
                        <input type="password" name="password" class="form-control" value="Student@123456" required />
                    </div>
                </div>
                <button type="submit" class="btn btn-primary w-100 py-2.5 fw-bold shadow-sm">
                    <i class="fa-solid fa-right-to-bracket me-2"></i>Sign In to Student Portal
                </button>
            </form>
            <div class="mt-4 text-center">
                <a href="/Admin/Account/Login" class="text-decoration-none small text-muted"><i class="fa-solid fa-shield-halved me-1"></i>Switch to Administrator Portal</a>
            </div>
        </div>
    </div>
</body>
</html>
